# ==============================================================================
# HERMES APP — Dependencias Python
# ==============================================================================
# Instalar con:
#   pip install -r REQUIREMENTS.txt
#
# Nota: uuid, json, hashlib, datetime, os, re, sys, threading,
#       typing y tkinter son parte de la librería estándar de Python 3
#       y NO necesitan instalarse con pip.
#       En Linux, tkinter requiere: sudo apt install python3-tk
# ==============================================================================


# ------------------------------------------------------------------------------
# Framework web y servidor ASGI
# ------------------------------------------------------------------------------
fastapi>=0.135.1
uvicorn[standard]>=0.42.0


# ------------------------------------------------------------------------------
# Validación de datos — Pydantic v2
# Usado directamente en todos los schemas (schemas/users.py, chat.py, etc.)
# ------------------------------------------------------------------------------
pydantic>=2.12.5


# ------------------------------------------------------------------------------
# Base de datos — PostgreSQL async
# SQLAlchemy 2.0 con soporte async + driver asyncpg para PostgreSQL
# ------------------------------------------------------------------------------
sqlalchemy[asyncio]>=2.0.48
asyncpg>=0.31.0


# ------------------------------------------------------------------------------
# Cache y ranking en tiempo real — Redis async
# Usado en redis_operations.py para cache de chat, ranking (Sorted Sets) y sesiones
# ------------------------------------------------------------------------------
redis[hiredis]>=7.3.0


# ------------------------------------------------------------------------------
# Autenticación — Firebase Admin SDK
# Verifica tokens de Firebase Auth (Google OAuth + email/password)
# ------------------------------------------------------------------------------
firebase-admin>=7.3.0


# ------------------------------------------------------------------------------
# Variables de entorno
# Lee el archivo .env para DATABASE_URL, REDIS_URL, GEMINI_API_KEY, etc.
# ------------------------------------------------------------------------------
python-dotenv>=1.2.2


# ------------------------------------------------------------------------------
# Google Calendar API
# OAuth2 + cliente de la API para leer y crear eventos del calendario del usuario
# Usado en quickstart.py (Alan + Ángel)
# ------------------------------------------------------------------------------
google-auth>=2.49.1
google-auth-oauthlib>=1.3.0
google-api-python-client>=2.193.0


# ------------------------------------------------------------------------------
# Google Gemini (IA)
# SDK oficial para conectar con Gemini 2.0 Flash (chatbot + function calling)
# Usado en services/llm_orchestrator.py y services/gemini_agent.py (Víctor)
# También usado en HermesAgent/hermes_app.py (prototipo)
# ------------------------------------------------------------------------------
google-genai>=1.68.0


# ==============================================================================
# PROTOTIPO DE ESCRITORIO — HermesAgent (backend/app/HermesAgent/hermes_app.py)
# Solo necesario si quieres correr el prototipo local de Víctor.
# El servidor FastAPI NO requiere estas librerías.
# ==============================================================================

# Síntesis de voz (texto → audio .mp3)
gTTS>=2.5.4

# Reproducción de audio en segundo plano
pygame>=2.6.1

# Captura de micrófono
sounddevice>=0.5.5

# Procesamiento matemático de audio (requerido por sounddevice)
scipy>=1.17.1
